Berserk VPN Bot
Version: v5.6.4 User Detail Cleanup + Smart Admin UI Cleanup
Build Type: Safe Full Source Patch / Original 22 Files
Date: 2026-07-06

هدف این پچ:
- خلوت‌سازی جزئیات کاربر و تبدیل صفحه اصلی به خلاصه مدیریتی.
- حذف نمایش لینک کوتاه از صفحه اصلی جزئیات کاربر.
- ادغام «خریدها» و «سرویس‌ها» در بخش جدید «📦 سرویس‌ها و خریدها».
- ادغام «شارژها» و «تراکنش‌ها» در بخش جدید «💰 مالی و کیف پول» برای جلوگیری از تکرار اطلاعات.
- نمایش جزئیات هر خرید در صفحه جداگانه همراه با سرویس‌های همان خرید.
- نمایش لینک کامل فقط داخل جزئیات همان سرویس.
- انتقال عملیات ارسال لینک، ارسال QR و بازگردانی به استخر به صفحه جزئیات همان سرویس.
- حذف/کوتاه‌سازی عملیات سریع شلوغ روی همه سرویس‌های کاربر.
- بهبود پاک‌سازی هوشمند پیام‌های موقت، لیستی و منویی پنل ادمین هنگام باز شدن بخش جدید.
- اضافه شدن نوع پیام در bot_messages برای پاک‌سازی context-based و جلوگیری از حذف پیام‌های مهم.

فایل‌های تغییر کرده نسبت به v5.6.3:
- README.md
- VERSION.txt
- admin.py
- db.py

تست‌ها:
- python -m py_compile *.py
- تست init دیتابیس با DB تستی
- تست migration ستون kind برای bot_messages
- تست ساخت ZIP با همان ساختار ۲۲ فایلی

نکته عملیاتی:
قبل از نصب روی سرور اصلی، از دیتابیس فعلی بک‌آپ بگیرید و نسخه را ابتدا روی محیط تست اجرا کنید.
