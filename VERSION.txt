Berserk VPN Bot
Version: v5.5.2 Message Defaults + RePool Tools
Build Type: Safe Full Source Patch / Original 22 Files
Date: 2026-07-04

هدف این پچ:
- بهتر شدن مدیریت متن‌ها: ادمین بتواند متن پیش‌فرض را ببیند، آن را به Draft کپی کند، و روی همان قالب ویرایش امن انجام دهد.
- محافظت از پیام‌های سیستمی/داینامیک مثل خرید سرویس، کیف پول و دعوت دوستان با کد {body}.
- اضافه شدن امکان بازگرداندن لینک تحویل‌شده به استخر، بدون ساخت لینک تکراری، برای موارد تست/تحویل اشتباه/اصلاح دستی.

اصلاحات مدیریت متن‌ها:
- دکمه «مشاهده متن پیش‌فرض» اضافه شد.
- دکمه «کپی پیش‌فرض به Draft» اضافه شد.
- در پیام‌های سیستمی، {body} نماینده بخش خودکار ربات است؛ مثل قیمت، موجودی، کسری موجودی، لینک دعوت و آمار کاربر.
- اگر ادمین در پیام‌های سیستمی {body} را حذف کند، ربات برای جلوگیری از خراب شدن خرید/کیف پول/دعوت، متن سیستمی را خودکار زیر متن ادمین اضافه می‌کند.
- پیش‌نمایش Draft برای پیام‌های سیستمی با اطلاعات نمونه نمایش داده می‌شود.

اصلاحات مدیریت لینک‌ها:
- در جزئیات لینک تحویل‌شده، گزینه «بازگردانی به استخر» اضافه شد.
- با تأیید ادمین، لینک از سرویس‌های کاربر حذف می‌شود و همان رکورد لینک دوباره available می‌شود.
- هیچ رکورد تکراری ساخته نمی‌شود.
- عملیات در ledger ثبت می‌شود و purchase_items در صورت وجود ستون‌های جدید، به حالت returned_to_pool علامت‌گذاری می‌شود.

فایل‌های تغییر کرده:
- admin.py
- db.py
- messages.py
- subs.py
- VERSION.txt

تست‌ها:
- python -m py_compile *.py
- تست init دیتابیس با DB تستی
- تست رندر پیام‌های سیستمی با {body}
- تست خرید آزمایشی و بازگرداندن لینک تحویل‌شده به استخر

نکته عملیاتی:
قبل از نصب روی سرور اصلی، از دیتابیس فعلی بک‌آپ بگیرید و نسخه را ابتدا روی محیط تست اجرا کنید.
