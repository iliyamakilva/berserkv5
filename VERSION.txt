Berserk VPN Bot
Version: v6.1.1 YouPanel Device Limits
Build Type: Full Source / Original 22 Files Preserved
Schema Version: 610
Date: 2026-07-12

هدف نسخه:
- تکمیل مدیریت محدودیت تعداد دستگاه برای پلن‌های YouPanel و اکانت تست.
- حفظ کامل فروش استخری قبلی و افزودن فروش خودکار YouPanel در کنار آن.
- ساخت خودکار سرویس با حجم، مدت و نوع شروع اعتبار اختصاصی هر پلن.
- ساخت اکانت تست ۲۰۰ مگ یک‌روزه یا مقادیر قابل تنظیم، فقط یک‌بار برای هر Telegram ID.
- مدیریت مصرف، Reset Usage، Revoke Subscription و حذف سرویس پنلی از داخل ادمین.
- حفظ اتمیک بودن کیف پول و refund امن در شکست Provisioning.
- نگهداری JWT فقط در حافظه و خواندن رمز پنل فقط از Environment.

تغییرهای مهم:
- Schema دیتابیس از 600 به 610 ارتقا یافت.
- plans: delivery_type و تنظیمات پنلی مستقل.
- subs: اطلاعات provider، panel username/status/usage/expiry و trial.
- purchases: provider، provision_error، completed_at و refunded_at.
- جدول trial_claims اضافه شد.
- مسیر خرید بر اساس pool یا youpanel dispatch می‌شود.
- سفارش پنلی در وضعیت provisioning رزرو و پس از دریافت همه لینک‌ها completed می‌شود.
- شکست ساخت باعث حذف best-effort حساب نیمه‌ساخته و refund کیف پول می‌شود.
- سفارش نیمه‌کاره بعد از Restart بازیابی و refund می‌شود.
- دکمه سیستمی اکانت تست اضافه شد.
- عملیات پنلی Usage/Reset/Revoke/Delete به جزئیات سرویس ادمین اضافه شد.
- افزودن لینک دستی به پلن خودکار مسدود شد.
- انتخاب سقف دستگاه به ویزارد ساخت سریع پلن افزوده شد.
- سقف دستگاه در جزئیات و پیش‌نمایش پلن نمایش داده می‌شود.
- YOUPANEL_TRIAL_MAX_DEVICES برای محدودیت دستگاه اکانت تست اضافه شد.

APIهای پشتیبانی‌شده:
- POST /api/admin/token
- POST /api/user
- DELETE /api/user/{username}
- GET /api/user/{username}/usage
- POST /api/user/{username}/reset
- POST /api/user/{username}/revoke_sub
- GET /api/admin

تست‌ها:
- py_compile تمام فایل‌های Python
- DB init و migration 600→610
- خرید استخری
- خرید پنلی موفق، چندتایی، failure و refund
- recovery سفارش provisioning
- Trial one-per-user و retry پس از failure
- Mock API Login/Create/Usage/Reset/Revoke/Delete
- جلوگیری از لینک دستی برای پلن YouPanel
- ZIP integrity و حفظ دقیق ۲۲ فایل

محدودیت:
تست destructive زنده با پنل واقعی، توکن واقعی تلگرام و پرداخت واقعی انجام نشده است. ابتدا روی پلن و کاربر آزمایشی Deploy و بررسی شود.
